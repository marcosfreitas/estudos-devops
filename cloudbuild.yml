steps:

- id: "Build de imagem"
  name: "docker"
  args: ['build', '.', '--tag', 'gcr.io/$PROJECT_ID/desafio_ci_go:latest']

- id: "Docker compose up"
  name: "docker/compose"
  args: ['up', '-d']

- id: "Executando teste unitário"
  name: "docker"
  args: ["exec", "-t", "desafio_ci_go", "go", "test"]

images:
  - 'gcr.io/$PROJECT_ID/desafio_ci_go:latest'